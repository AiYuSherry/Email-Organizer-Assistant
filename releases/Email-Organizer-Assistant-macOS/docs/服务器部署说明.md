# 📧 邮件助手 - 百度云服务器部署指南

## 🎯 目标
将邮件助手程序部署到百度云服务器（`120.48.76.86`），实现 24 小时自动运行，关机也能收到邮件报告。

---

## ⚡ 快速部署（推荐）

### 方法一：一键脚本部署（最简单）

**Step 1: 确保你能 SSH 登录服务器**

```bash
ssh root@120.48.76.86
```

输入密码后，如果能登录成功，就可以继续。

**Step 2: 在本地 Mac 运行一键部署脚本**

打开终端，执行：

```bash
cd "/Users/huziyang/Desktop/计算机/邮件助手"
bash upload_and_deploy.sh
```

这个脚本会自动：
- ✅ 上传所有必要文件到服务器
- ✅ 安装 Python 和依赖
- ✅ 配置 Systemd 定时任务
- ✅ 启动服务

---

### 方法二：手动分步部署

如果一键脚本遇到问题，可以手动操作：

**Step 1: 上传文件**

在本地 Mac 终端执行：

```bash
cd "/Users/huziyang/Desktop/计算机/邮件助手"

# 创建远程目录
ssh root@120.48.76.86 "mkdir -p /opt/email-assistant"

# 上传文件
scp final_email_assistant.py root@120.48.76.86:/opt/email-assistant/
scp daily_summary.py root@120.48.76.86:/opt/email-assistant/
scp config.json root@120.48.76.86:/opt/email-assistant/
scp deploy.sh root@120.48.76.86:/tmp/
```

**Step 2: 在服务器上执行部署脚本**

```bash
ssh root@120.48.76.86
bash /tmp/deploy.sh
```

---

## 🧪 测试部署

部署完成后，建议手动测试一下：

```bash
# SSH 登录服务器
ssh root@120.48.76.86

# 切换到程序目录
cd /opt/email-assistant

# 手动运行日报程序测试
sudo /opt/email-assistant/venv/bin/python3 daily_summary.py
```

如果看到以下输出，说明部署成功：
```
✅ 邮件发送成功！
```

---

## 📋 管理和维护

### 查看定时任务状态

```bash
ssh root@120.48.76.86

# 查看定时器状态
systemctl list-timers | grep email

# 输出示例：
# email-assistant-daily.timer  email-assistant-daily.service  Tue 2026-03-18 23:30:00 CST
# email-assistant-main.timer   email-assistant-main.service   Tue 2026-03-18 23:00:00 CST
```

### 查看日志

```bash
ssh root@120.48.76.86

# 查看主程序日志（23:00 运行）
tail -f /var/log/email-assistant/main.log

# 查看日报日志（23:30 运行）
tail -f /var/log/email-assistant/daily.log

# 查看错误日志
tail -f /var/log/email-assistant/main.error.log
tail -f /var/log/email-assistant/daily.error.log
```

### 手动运行程序

```bash
ssh root@120.48.76.86

# 手动运行主程序
systemctl start email-assistant-main

# 手动运行日报
systemctl start email-assistant-daily
```

### 停止/重启服务

```bash
ssh root@120.48.76.86

# 停止定时器
systemctl stop email-assistant-main.timer
systemctl stop email-assistant-daily.timer

# 重启定时器
systemctl restart email-assistant-main.timer
systemctl restart email-assistant-daily.timer

# 禁用自动启动
systemctl disable email-assistant-main.timer
systemctl disable email-assistant-daily.timer

# 启用自动启动
systemctl enable email-assistant-main.timer
systemctl enable email-assistant-daily.timer
```

---

## 🔧 更新程序

如果修改了本地程序，需要更新到服务器：

```bash
cd "/Users/huziyang/Desktop/计算机/邮件助手"

# 上传更新后的文件
scp final_email_assistant.py root@120.48.76.86:/opt/email-assistant/
scp daily_summary.py root@120.48.76.86:/opt/email-assistant/

# 重启服务（SSH 登录后执行）
ssh root@120.48.76.86 "systemctl restart email-assistant-main.timer email-assistant-daily.timer"
```

---

## 🛠️ 故障排查

### 问题 1: 邮件发送失败

**排查步骤：**

```bash
ssh root@120.48.76.86

# 检查配置是否正确
cat /opt/email-assistant/config.json

# 手动运行程序查看详细错误
cd /opt/email-assistant
sudo /opt/email-assistant/venv/bin/python3 daily_summary.py
```

**常见原因：**
- QQ 邮箱授权码过期 → 需要在 QQ 邮箱重新生成
- 服务器防火墙阻止出站连接 → 检查 `iptables` 或安全组设置

### 问题 2: 定时任务不运行

**排查步骤：**

```bash
ssh root@120.48.76.86

# 检查定时器状态
systemctl status email-assistant-main.timer
systemctl status email-assistant-daily.timer

# 检查服务状态
systemctl status email-assistant-main.service
systemctl status email-assistant-daily.service

# 查看 Systemd 日志
journalctl -u email-assistant-main.service -n 50
journalctl -u email-assistant-daily.service -n 50
```

### 问题 3: 程序运行但收不到邮件

**检查邮件是否被分类到垃圾箱：**
- 检查 QQ 邮箱垃圾箱
- 检查邮件是否被自动归类到其他文件夹

---

## 📁 文件说明

| 文件 | 说明 |
|------|------|
| `final_email_assistant.py` | 主程序 - 详细分析近期邮件 |
| `daily_summary.py` | 日报程序 - 分析今天邮件 |
| `config.json` | 配置文件 - 包含邮箱和 API 密钥 |
| `deploy.sh` | 服务器部署脚本 |
| `upload_and_deploy.sh` | 本地一键上传部署脚本 |

---

## ⏰ 运行时间表

| 时间 | 程序 | 功能 |
|------|------|------|
| 23:00 | final_email_assistant.py | 分析近期邮件（详细版） |
| 23:30 | daily_summary.py | 分析今日邮件（日报） |

---

## ✅ 部署检查清单

部署完成后，确认以下事项：

- [ ] 可以 SSH 登录服务器
- [ ] 文件已上传到 `/opt/email-assistant/`
- [ ] 手动测试成功发送邮件
- [ ] 定时任务已启用（`systemctl list-timers` 能看到）
- [ ] 日志目录可写入（`/var/log/email-assistant/`）

---

## 🎉 完成！

部署完成后，你的邮件助手将在云端 24 小时运行：
- ✅ 每天 23:00 收到详细邮件分析
- ✅ 每天 23:30 收到今日邮件日报
- ✅ 本地电脑关机不影响服务

**现在可以关闭电脑，邮件助手会在云端继续工作！**
